# 🚨 ПРОБЛЕМА: Отсутствуют файлы frontend на сервере

## ❗ Симптомы:
```
failed to calculate checksum of ref ... "/frontend/public": not found
```

## 🔍 Причина:
На сервере отсутствуют файлы папки `frontend/` в репозитории.

## 🛠️ Быстрое исправление:

### Шаг 1: Проверьте наличие файлов
```bash
cd ~/Habbits
ls -la frontend/
ls -la frontend/public/
```

### Шаг 2: Если файлов нет - запустите исправление
```bash
# Скачайте скрипт исправления
curl -O https://raw.githubusercontent.com/your-username/habits-tracker/main/fix-server-files.sh
chmod +x fix-server-files.sh

# Запустите исправление
./fix-server-files.sh
```

### Шаг 3: Альтернатива - пересоздайте репозиторий
```bash
cd ~
rm -rf Habbits
git clone https://github.com/your-username/habits-tracker.git Habbits
cd Habbits
```

### Шаг 4: Проверьте и запустите
```bash
# Проверьте файлы
ls -la frontend/public/

# Очистите Docker
docker system prune -f

# Запустите развертывание
./deploy.sh
```

## ✅ Проверка работоспособности:

После исправления файлы должны быть:
- ✅ `frontend/package.json`
- ✅ `frontend/public/index.html`
- ✅ `frontend/src/App.js`

## 🎯 Результат:
Теперь `./deploy.sh` должен работать без ошибок копирования файлов!
